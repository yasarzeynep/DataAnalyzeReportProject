@page "/"

<style>
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f4f4f4;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
    }

    .welcome-container {
        text-align: center;
        background-color: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    h3 {
        color: #007bff;
        margin-bottom: 20px;
    }

    .nav-links {
        display: flex;
        flex-direction: column;
        gap: 16px;
        margin-top: 16px;
    }

    .nav-link {
        text-decoration: none;
        padding: 12px 20px;
        border: 2px solid #007bff;
        border-radius: 8px;
        color: #007bff;
        display: flex;
        align-items: center;
        transition: background-color 0.3s, color 0.3s;
    }

        .nav-link:hover {
            background-color: #007bff;
            color: #fff;
        }

    .icon {
        margin-right: 12px;
        font-size: 20px;
    }
</style>

<div class="welcome-container">
    <h3>Welcome!</h3>
    <p>How can I assist you today? Please choose one of the options below:</p>

    <div class="nav-links">
        <NavLink href="excel-upload" class="nav-link">
            <span class="icon">📊</span> Upload Excel
        </NavLink>
        <NavLink href="link-analysis" class="nav-link">
            <span class="icon">🔗</span> Link Analysis
        </NavLink>
    </div>
</div>
